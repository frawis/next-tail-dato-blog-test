module.exports = {
    eslint: {
      dirs: ['pages', 'src/utils', 'src/components', 'src/lib'], // Only run ESLint on the 'pages' and 'utils' directories during production builds (next build)
    },
  }